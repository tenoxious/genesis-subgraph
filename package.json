{
  "name": "genesis-project",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ genesis-project",
    "create-local": "graph create --node http://localhost:8020/ genesis-project",
    "remove-local": "graph remove --node http://localhost:8020/ genesis-project",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 genesis-project"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.0",
    "@graphprotocol/graph-ts": "0.22.0"
  }
}
